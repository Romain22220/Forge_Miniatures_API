<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Properties>
        <Property name="LOG_PATH">logs</Property>
        <Property name="APP_NAME">forge-miniature-api</Property>
    </Properties>

    <Appenders>

        <!-- Console -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="%style{%d{yyyy-MM-dd HH:mm:ss}}{green} %highlight{%-5level} [%style{%t}{cyan}] %style{%c{1}}{yellow} - %style{%msg}{magenta}%n"/>
        </Console>

        <!-- Fichier texte -->
        <RollingFile name="FileLogger"
                     fileName="${LOG_PATH}/application.log"
                     filePattern="${LOG_PATH}/application-%d{yyyy-MM-dd}.log.gz">

            <PatternLayout
                    pattern="%style{%d{HH:mm:ss.SSS}}{green} %highlight{%-5level} [%style{%t}{cyan}] %style{%c{1}}{yellow} - %style{%msg}{magenta}%n"/>

            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>

        </RollingFile>

        <!-- JSON pour Kibana -->
        <RollingFile name="JsonLogger"
                     fileName="${LOG_PATH}/application.json"
                     filePattern="${LOG_PATH}/application-%d{yyyy-MM-dd}.json.gz">

            <JsonTemplateLayout eventTemplateUri="classpath:LogstashJsonEventLayoutV1.json"/>

            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>

        </RollingFile>

    </Appenders>

    <Loggers>

        <!-- Spring -->
        <Logger name="org.springframework" level="INFO"/>

        <!-- Security -->
        <Logger name="org.springframework.security" level="DEBUG"/>

        <!-- Ton package -->
        <Logger name="com.forge_miniatures" level="DEBUG"/>

        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLogger"/>
            <AppenderRef ref="JsonLogger"/>
        </Root>

    </Loggers>

</Configuration>